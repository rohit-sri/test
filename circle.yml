---
machine:
   php:
   version: 5.6.30
   mysql:
   vesrion: 5.5.47
dependencies:
   pre:
      - sudo apt-add-repository -y ppa:ondrej/php
      - sudo apt-get -y update
      - sudo apt-get -y install php5.6
test:
   override:
      - php --version
