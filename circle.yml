---
machine:
      service:
         - docker
dependencies:
      - docker build -t 174688301618.dkr.ecr.us-east-1.amazonaws.com/test:$CIRCLE_SHA1
test:
      post:
         - docker run -d -p 8080:8080 --name test 174688301618.dkr.ecr.us-east-1.amazonaws.com/test:$CIRCLE_SHA1 sleep 10
         - curl --retry 10 --retry-delay 5 localhost:8080 | grep "Hello World!"
