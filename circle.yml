---
machine:
   php:
   version: 5.6.30
   mysql:
   vesrion: 5.5.47
   test:
      override:
         - mkdir -p $CIRCLE_TEST_REPORT/phpunit
         - phpunit --log-junit $CIRCLE_TEST_REPORT/phpunit/junit.xml tests
